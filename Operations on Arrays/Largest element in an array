def largest(arr,n):
    max = arr[0]
    for i in range(1, n):
        if arr[i] > max:
            max = arr[i]
    return max
  
arr = [10, 324, 45, 90, 9808]
n = len(arr)
Ans = largest(arr,n)
print ("Largest in given array is",Ans)



Time complexity:
  Naive approach = O(n^2)
    Best approach = Θ(n)
    Worst approach = Θ(n^2) or O(n^2)
  Efficient approach = Θ(n)
  
Time complexity for sorted array:
  Naive approach = O(n^2), worst case
  Efficient approach = Θ(n)
